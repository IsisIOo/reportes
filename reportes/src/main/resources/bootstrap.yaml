spring:
  application:
    name: reportes
  cloud:
    config:
      enabled: true
      uri: http://config-server-service:8081
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect